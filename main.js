// main.js

document.addEventListener('DOMContentLoaded', () => {
  const typed = new Typed('.text', {
    strings: ["a Developer", "a Designer", "a CSE Student"],
    typeSpeed: 100,
    backSpeed: 100,
    backDelay: 1000,
    loop: true
  });
});
